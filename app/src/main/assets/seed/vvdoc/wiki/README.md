# vvdoc Offline Fixture

這是 VV Wiki 的離線測試資料。請使用 Library 瀏覽、Search 搜尋，再開啟 Reader。

- [[Obsidian_mini_test]] 展示 Obsidian_mini 相容 Markdown。
- 可切換 rendered/raw source，所有內容留在手機本機。

> [!TIP] 測試提示
> 試著搜尋 `Mermaid`、`KaTeX` 或 `task list`。
